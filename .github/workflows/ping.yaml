name: Ping Server

on:
  schedule:
    - cron: "*/5 * * * *" # every 1 minute
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request
        run: curl -s https://chitzy-server.onrender.com/ping
